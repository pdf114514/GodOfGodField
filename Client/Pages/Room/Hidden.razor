@page "/rooms/hidden"

@code {
    string RoomId = string.Empty;

    protected override async Task OnInitializedAsync() {
        RoomId = await Api.CreateRoom();
        await Api.AddRoomUser(new() { RoomId = RoomId, UserName = AppState.UserName });
        await JS.Alert("Room joined!");
    }
}